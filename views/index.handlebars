<h1>Time to eat da burger...</h1>
<p>The 300 burgers in 30 days challenge. Can you handle it?</p>
<div class='container'>
  <div class='row'>
    {{!-- left section --}}
    <div class='col-md-5'>
      <h2>Burgers to eat!</h2>
        {{#each burger}} 
          {{#unless devoured}}
            <p class='text-left'> {{> burger/burger-block}} #{{id }}: {{burger_name}}</p>
          {{/unless}}
        {{/each}}
    </div>
    {{!-- middle section with submit button --}}
    <div class='col-md-2'>
      <form class="create-form">
        <div class="form-group">
          <label for="addburger" class='text-warning' >Burger Name:</label>
          <input type="text" id="addburger" name="name">
        </div>
        <button type="submit" class= 'btn btn-lg btn-outline-light' id='submit'>Add Burger</button>
      </form>
    </div>
    {{!-- right section --}}
    <div class='col-md-5'>
      <h2>Burgers devoured!</h2>
        {{#each burger}}
          {{#if devoured}}
            <p class='text-left'> {{> burger/burger-delete}}  #{{id }}: {{burger_name}} </p> 
          {{/if}}
        {{/each}}
    </div>
  </div>
</div>